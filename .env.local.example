# Copy to .env.local for local frontend development.
# Points the Next.js app at a separately-running backend (uvicorn on :8000).
# Leave empty/unset in Vercel -- there the frontend and the Python API
# share one deployment, so relative /api/* requests already resolve.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
